.row
  .col-md-8.col-md-offset-2.col-lg-8.col-lg-offset-2
    %h3 comment form

    = form_for([@movie, @movie.comments.build]) do |f|
      = f.text_area :body, class: "form-control", placeholder: "You can add new comment here"
      = f.submit "Comment"